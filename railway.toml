[build]
builder = "NIXPACKS"
buildCommand = "cd server && pip install -r requirements.txt"

[deploy]
startCommand = "cd server && python -m spacy download en_core_web_sm && uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}"
